import { SCLoader } from "./Loader.styled";

export const Loader = () => {
  return (
    <SCLoader>
      <div className="loader">
        <div className="circle"></div>
      </div>
    </SCLoader>
  );
};
